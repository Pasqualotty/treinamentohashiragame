# Godot 4+ specific ignores
.godot/
.nomedia

# Godot-specific ignores
*.translation

# Imported translations (automatically generated from CSV files)
.import/

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Export
export/*.apk
export/*.aab
export/*.zip
*.keystore
*.jks

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*~

# Claude Code — worktrees de agente sao repos embutidos: sem esta regra,
# um `git add -A` na raiz durante trabalho multi-frente commita gitlink
# quebrado. Settings locais tambem nao vao pro repo.
.claude/worktrees/
.claude/settings.local.json

# Staging Imagine (JPEG/.png cru) — só entra o PNG chroma em assets/
tools/_staging_elenco/

export/*.idsig

# Staging Imagine (JPEG mentido) — só Pillow PNG entra em characters/
assets/_staging_anim_quarteto/

